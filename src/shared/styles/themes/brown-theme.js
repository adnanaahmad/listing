import { createTheme } from '@mui/material/styles';

export const BrownTheme = createTheme({
  palette: {
    primary: {
      main: '#9b551e',
    },
    contrastThreshold: 3,
    tonalOffset: 0.2,
  },
});