import { createTheme } from '@mui/material/styles';

export const GreenTheme = createTheme({
  palette: {
    primary: {
      main: '#65764D',
    },
    contrastThreshold: 3,
    tonalOffset: 0.2,
  },
});